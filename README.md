# How to use

```
-- Help Message --
Usage: XmlDeserialization.exe [OPTIONS]
Options:
  -d, --dir=VALUE            Input directory name (Default directory is a
                               current directory)
  -h, --help                 Show help
```